import {spawnSync} from 'node:child_process';import path from 'node:path';import {resolvePython,savePython,projectRoot} from './python-resolver.mjs';
console.log('Locating a real system Python installation...');const python=resolvePython({ensurePip:true,requirePip:true,useConfig:false,minMinor:10,maxMinor:12});if(!python){console.error('\nNo pip-capable system Python was found.');console.error('ClipperX ignored the Hermes agent virtual environment because it is not a usable app runtime.');console.error('Install Python 3.11 or 3.12 from python.org with “Add Python to PATH” and “pip” enabled, then run this file again.');process.exit(1);}console.log(`Using Python ${python.version}: ${python.executable}`);savePython(python);
console.log('Installing ClipperX Python engine dependencies...');const userArgs=python.venv?[]:['--user'];const result=spawnSync(python.executable,['-m','pip','install','--disable-pip-version-check',...userArgs,'-r',path.join(projectRoot,'requirements.txt')],{cwd:projectRoot,stdio:'inherit',windowsHide:true});if(result.error||result.status!==0){console.error('\nEngine setup failed. Try this exact command:');console.error(`"${python.executable}" -m pip install ${userArgs.join(' ')} -r requirements.txt`);process.exit(1);}savePython(python);console.log('\nClipperX Python engine installed and interpreter saved.');
