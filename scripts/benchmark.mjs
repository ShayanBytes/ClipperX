import {spawnSync} from 'node:child_process';import {resolvePython} from './python-resolver.mjs';const python=resolvePython({requirePip:false});if(!python){console.error('No system Python found. Run npm run setup:engine.');process.exit(1);}const args=['-m','engine.reliability_benchmark',...process.argv.slice(2)];const result=spawnSync(python.executable,args,{cwd:process.cwd(),stdio:'inherit',windowsHide:true});process.exitCode=result.status??1;
