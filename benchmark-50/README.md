# ClipperX 50-take benchmark

Put each source clip at `<case>/input.mp4`, render ClipperX to `<case>/candidate`, create baselines, review all four views, then run evaluation. A release cannot pass with pending cases.
